// PURE FUNCTION
// a) SAME INPUT === SAME OUTPUT (dla tego samego inputu ten sam output)
// b) NO SIDE EFFECTS (brak efektów ubocznych)
// 2. IMMUTABILITY
// 3. HOF (HIGHER-ORDER FUNCTION) - funkcja wyższego rzędu 
// INPUT OR/AND OUTPUT -> FUNCTION
// INPUT FUNCTION === CALLBACK [1, 2, 3].map((n) => n);
// OUTPUT FUNCTION === CLOSURE (fn) =>  fn;